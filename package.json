{
  "name": "pai-tools",
  "version": "0.2.21",
  "description": "PAI Issue Orchestrator — automated issue implementation with verification",
  "type": "module",
  "bin": {
    "pait": "./cli.ts"
  },
  "scripts": {
    "setup": "bun ./cli.ts setup",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "typecheck": "bunx tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SaintPepsi/pai-tools.git"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
